<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Базовый стиль для всех кнопок калькулятора -->
    <style name="CalcBtn" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:layout_rowWeight">1</item>
        <item name="android:textSize">28sp</item>
        <item name="android:textColor">@color/calc_btn_text</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:padding">0dp</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Стиль для цифровых кнопок -->
    <style name="CalcBtn.Num">
        <item name="android:backgroundTint">@color/calc_num_btn_bg</item>
    </style>

    <!-- Стиль для кнопок операторов -->
    <style name="CalcBtn.Op">
        <item name="android:backgroundTint">@color/calc_op_btn_bg</item>
        <item name="android:textSize">32sp</item>
    </style>

    <!-- Стиль для кнопок специальных функций -->
    <style name="CalcBtn.Special">
        <item name="android:backgroundTint">@color/calc_special_btn_bg</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>
